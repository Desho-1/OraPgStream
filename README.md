# OraPgStream
OraPgStream is a lightweight, agentless Change Data Capture (CDC) tool that streams data changes from Oracle to PostgreSQL in near real-time using LogMiner.  Built for simplicity and control, OraPgStream requires minimal setup and runs directly from your environment — no external services, no complex infrastructure.
